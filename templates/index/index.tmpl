<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
	<link rel="stylesheet" href="../css/style.css">
    <title>Главная</title>
</head>
<body>
index.tmpl
<a href="/public/news">новости</a>
<a href="/public/blog">блог</a>
<a href="/public">индекс</a>
<ul>
    {% for blog in blogsOne %}
        <li>{{ blog }}</li>
    {% endfor %}
</ul>

<ul>
    {% for blog1 in blogsAll %}
        <li>{{ blog1.id }}</li>
        <li>{{ blog1.title }}</li>
        <li>{{ blog1.content }}</li>
    {% endfor %}
</ul>

<ul>
    {% for news in oneNews %}
        <li>{{ news }}</li>
    {% endfor %}
</ul>




</body>
</html>